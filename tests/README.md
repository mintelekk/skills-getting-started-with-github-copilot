# Backend API Tests

All tests use pytest and follow the AAA (Arrange-Act-Assert) pattern.

## Running tests

1. Install dependencies:
   ```sh
   pip install -r requirements.txt
   ```
2. Run tests:
   ```sh
   pytest
   ```

## Test structure
- Each test is organized into Arrange, Act, and Assert sections.
- Tests cover all API endpoints and error cases.
